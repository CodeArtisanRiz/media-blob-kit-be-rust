services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: media-blob-kit-app
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
